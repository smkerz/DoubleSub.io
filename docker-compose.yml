services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - uploads:/app/uploads
      - app-data:/app/data

volumes:
  uploads:
  app-data:
